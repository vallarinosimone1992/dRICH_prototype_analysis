Not-orderd to do list:
-> Insert a time calibration data reading like CLAS12 rich.
-> 
-> Set DAQ to create a setup file, introduce a setup reading.
-> Plan to introduce the setup file reading.
-> Write the MPPC position function
-> Plan to introduce the SiPM reading. Do we will use different SiPM or the same configuration of 2021?
-> Write the MAPMT and MPPC channel reading.

What I'm doing:
MAPMT
fiber == 4, 5, 6, 7 -> MAROC 2.
Altre: MAROC 3.

fiber to PMT(fiber)

MAPMT position(ch, pmt, *x, *y, *r)



MAPMT MAROC 2: 	if (fiber == 4, 5, 6, 7) /UPSTREAM
			if(ch < 64) 	label = IN1_ch
			if(ch > 127) 	label = IN3_(ch-128)
				nCh = map1 at label.
		if (fiber == 8, 9, 10, 11) /DOWNSTREAM
			if(ch < 64)	label = IN1_(ch)
			if(ch > 127)	label = IN3_(ch-128)
				nCh = map2 at label.

MAPMT MAROC 3: if (fiber == 4, 5, 6, 7) /UPSTREAM
			if(ch < 64)	label = IN1_ch
			if(ch > 127)	label = IN3_(ch-128)
				nCh = map1 al label.
		if(fiber == 8, 9, 10, 11) /DOWNSTREAM
			if(63 < ch < 128)	label = IN1_(ch-64)
			if(ch > 127)		label = IN3_(ch-128)
				nCh map2 at label

